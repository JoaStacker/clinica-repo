import 'package:flutter/material.dart';

class LoginCommonWidget extends StatelessWidget {
  const LoginCommonWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(color:Colors.blue);
  }
}

